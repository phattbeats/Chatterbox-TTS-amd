# DEPRECATED — wheels.phatt.vip is offline.
# For --rocm-windows (INSTALL_ROCM_WINDOWS), start.py downloads wheels
# directly from https://github.com/phattbeats/Chatterbox-TTS-Server/releases/tag/v0.1.0-rocm-wheels
# and installs them locally. This file is no longer used.
# Keep here for reference only; do not use.